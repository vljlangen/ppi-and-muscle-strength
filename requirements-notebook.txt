# Minimal dependencies for ppi_vs_muscle_analysis.ipynb (Zenodo / reproducibility)
#
# Intended Python: Apple CLT 3.9.6 — create venv with:
#   /usr/bin/python3 -m venv .venv
#   .venv/bin/python -m pip install -U pip setuptools wheel
#   .venv/bin/pip install -r requirements-notebook.txt
#
# (pandas 3.x requires Python >= 3.11, so this file pins a 3.9-compatible stack.)

pandas==2.2.3
numpy==2.0.2
scipy==1.13.1
matplotlib==3.9.4
plotnine==0.13.6
mizani==0.11.4
statsmodels==0.14.6
patsy==1.0.2
tableone==0.9.6
tabulate==0.9.0
openpyxl==3.1.5
ipykernel==6.31.0
